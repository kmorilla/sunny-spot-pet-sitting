export interface IExperience {
  title: string;
  subTitle: string;
}

export interface IPackage {
  image: string;
  title: string;
  price: number;
  description: string;
}

export interface IContactInfo {
  matIcon: string;
  info: string;
}
